apiVersion: v1
kind: Service
metadata:
  name: audiodeskription-backend
  namespace: audiodeskription
  labels:
    app.kubernetes.io/name: audiodeskription
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: audiodeskription-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
